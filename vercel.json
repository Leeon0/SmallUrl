{
  "builds": [
    { "src": "cmd/main.go", "use": "@vercel/go" }
  ],
  "version": 2,
  "build": {
    "cmd": "go build -o app cmd/main.go"
  },
  "routes": [
    { "src": "/(.*)", "dest": "/app" }
  ]
}